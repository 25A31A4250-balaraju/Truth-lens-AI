[build]
  base    = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all routes return index.html so React Router doesn't 404
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
