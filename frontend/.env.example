# Backend API base URL — set this in your Netlify dashboard under Site → Environment Variables
# After deploying the FastAPI backend to Railway or Render, paste its URL here:
VITE_API_BASE_URL=https://your-backend.railway.app/api/v1
